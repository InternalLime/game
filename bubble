var int = [5, 4, 2, 3, 1];
for (var j = 0, len = int.length - 1; j < len; j++) {
    var swapped = false;
    var i = 0;
    while (i < len - j)
    {
        if (int[i] > int[i + 1]) 
        {
            var c = int[i];
            int[i] = int[i + 1];
            int[i + 1] = c;
            swapped = true;
        }
        i++;
    }
    if(!swapped)
     break;
}
console.log(int);


1создаем переменную из нескольких чисел
2 создается цикл.  (что за число len?) в цикле 4  выражения, что делает j<len, в какой момент исполняется это и для чего?
3   swapped это просто переменная или за
4  все понятно
5   с этой строчкой понятно,
7  эта строчка отвечает за смену местами чисел, если первое больше чес второе+1
9,10,11 - сдесь происходит пнепосредственно процесс смены местами
12 если хоть одна смена происходила, то swapped = true, и  цикл  не закончится в строчке  16,17
14  i получает + 1 цикл возвращается в строчку 5
16,17 если не одного перемещения чисел не произошло, то  цикл начинающийся со строчки 2 завершается
19 что означает .log ??  эта строчка отвечает за вывод результата?

Общие вопросы:
1 Для чего например  еще может использоваться  этот метод? (помимо расстановки чисел в нужном порядке)

2 если взять на премере нашей програмки  то этот метод можно использовать для проверки, не закрыт ли путь к монетке?