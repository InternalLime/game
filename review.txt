HTML
    - давай вынесем js в файл со скриптом
    - давай размер поля будем задавать глобальной переменной в скрипте, а не числом в HTML

JS
matrixArray
    - пробелы вокруг арифметических операций, присваиваний и знаков сравнения
    - Нужно понимать, что код
        matrix[x][y] = Math.floor((Math.random() *2)); 
        генерирует примерно половину единиц и половину нулей)
        а мы хотели задавть число единиц глобавльной переменной
    - так как мы работаем с глобальной переменной, то эта функция может ничего не возвращать

function print(m)
    - Философия: функция print - это функция, которая только выводит только то, что ей дают, она не должна менять входной параметр. А строка    m = new Array(); его меняет. И еще вопрос к тебе: Что такое new?
    Можешь изучить и рассказать мне как он работает?
    -   for(x=0; x<10; x++) - хочется что бы размер матрицы задавался константой глоабльной. Иначе дублирование код получает (много раз где пишем число 10) и большая вероятность ошибок (в одном месте 10 поменяли, а в другом нет) и поле нельзя задать динамически (например спросить у пользователя)
    - Зачем эта функция возвращает значение? 

Ответы на вопросы:
    1) Отревьювил, замечания выше.
    2) Не понял вопрос, но надо сделать как я написал с константой
    3) Скорее всего не весь js можно писать в HTML непосредственно в теге. После того, как перенес код в JS фвйл функция заработала. Советую не делать много действий в HTML а просто вызывать одну функций, назвать ее можно например init, и в ней выполнять всю инициализацию.
    4) Внутри функции, которая обарабатывает стрелки :) Там разве не очевидно? Какие именно проблемы? (стрелка вверх-вниз меняется y координата, напрмиер)
    5) Не понимаю что такое Xscan и Yscan и в чем отличие от x,y ?