HTML
    - две клетки с индексом 10,10

function input() 
    - вынеси обработку кнопок во внешнюю функцию
    - func37 неудачное название. Лучше назвать функции up down right left. Правилом хорошего тона считается не использовать числа в названиях переменных и функций.
    - за вырваниваем надо следить, а лучше сразу науиться правильно расставлять все отступы.

function matrixArray(rows,columns)
    - куча магических констант: 41, 11 - это очень не понятно.
    - Генерацию монеток и инициализацую матрицы надо вынести в отдельныефункции.
    - зачем тебе условие if(u==0)? Подумай почему его можно убрать?
    - i = Math.floor((Math.random() *11)); генерирует число от 0 до 10, а ты используешь только элементы матрицы от 1 до 10. Я бы советовал использовать для координат числа от 0 до 9. Так удобней просто. И это общее соглашение, что нумерация всего и вся происходит начиная с нуля. Некоторые любят ее нарушать, но этого делать не стоит, я думаю.
    - число монеток это константа, которая должна быть в начале файла

function print(m)
    - нет пробелов вокруг операций сравнения
    - зачем эта функция возвращает значение?


function func37()
    - scan встречается в обоих частях условия ее можно вынести

function scan()
    - эта функция по сути обновляет картинку и все изображения, для такой функции самое подходящее название update

Общие замечания:
    - зачем нужна строка coordinates = 0;?
    - теперь старайся делать на каждое замечание по коммиту.
    - Ты реализовал не плохо все по сути. Содержание подкорректируем. Но тут мы столкнулись с одной из очень важных проблем программирования - масштабируемостью. Т.е. смотри, а если ты захочешь чтобы поле было 5 на 5? А если 15 на 15? При таком подходе придется все переписывать. Мы же постараемся сделать в будуещем так, чтобы подобные изменения задавали одной константой. Или вообща вводились с клавиатуры. Но сначала давай отладим этот вариант. И скажем что это первая версия. Ок? Сделаем так, потамучто второй подход очевдно сложнее и сначала приведем код в порядок.



Вопросы:
1) Программка загружается зачастую не с первого раза, нужно несколько раз обновить страницу,  до того момента, как матрица не появится, чтобы  заработало.
    - Я думаю, что это связано с самым первым замечанием. Когда монетка попадает в клетку 9,10 то происходит ошибка, так как такой улетки нет. Я это нашел с помощью инструментов разработчка обновляя страницу.
2) Генерация именно определенного количества монеток, тоже странно работает, можешь дать совет, что там не так?
    - У тебя генируется монетка иногда для нулевой координаты и по этому монеток иногда получается меньше, чем задано.
3) В координатах X9Y10, программка зависает
    - Снова первое замечание)))))
4) Я понимаю, что как-то можно сделать, чтобы в файле game все блоки генерировались сами  ,а не я их создавал, но как?
    - Глава 15 в книге. 15.17, 15.18, 15.18. Но лучше прочитать главу полностью. А пока читаешь привести код в порядок без динамического генерирования.