- best_func и func не лучшие названия функций, я их писал для примера,  абстрагируясь от задачи.

function func(evt)
    - if (evt.keyCode == 39) - выравнивание

function matrixArray(rows,columns)
    -   for( var x=0;  x<columns; x++)   ->   for(var x = 0; x < columns; x++) и остальные циклы должны быть в таком же виде

function  generate(object)
    - function  generate(object) лишний пробел
    - coordinates = x+","+y; пробелы вокруг +
    - и вокруг осатльных бинарных операций должны быть пробелы


Пример динамической генерации в отдельном файле example_dynamic_generation.html


