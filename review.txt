Ответы на вопросы:

1)---зачем тебе условие if(u==0)? Подумай почему его можно убрать?
    - Ответ: а если у нас получается, что  подобраные случайные координаты совпадут? нам же нужно именно определенное количество монеток, а так получится что будет 1 монетка на одной клетке вместо двух на 2 разных. ОБщее количество монеток станет меньше, чем нужно.
    - Смотри, сначала у нас матрица полностью нулевая и на первой итерации мы не попадем в блок else первого if, а далее у нас будет как минимум одна монетка на поле, соответственно число already_have_bomb никогда не будет нулем.

2)---вынеси обработку кнопок во внешнюю функцию
    - Ответ: Не понимаю что ты имеешь в виду.
    - примерно вот это:
        function func(evt) { ... }
        function best_func() 
        {
            document.onkeydown = func;

        }

3)---зачем эта функция возвращает значение?
    - Ответ: а где именно?
    - Я не в той функции написал. Имел в виду вот эту function matrixArray(rows,columns)

Замечания:

function input() 
    - вынеси обработку кнопок во внешнюю функцию
    - можно просто left, right, up, down.

function matrixArray(rows,columns)
    - чем отличаются coins() и bombs()? Может быть их можно как-то скомбинировать? А то получается дублирование кода. Можно параметр (число 1 или 2) передавать как аргумент. А числа 1 и 2 запихнуть в константы coin = 1 bomb = 2, а функцию генерации назвать generate. Тогда получится записать так:
        generate(coun);
        generate(bomb);
    и кода будет меньше и вероятность сделать ошибку меньше.

    

function function_right()
    - код document.getElementById("inputValue").value = "X="+ Xscan + "Y=" + Yscan; встречается в обеиз частях условия, его можно вынести. Аналогично с другими походими функциями.
    - код "X="+ Xscan + "Y=" + Yscan; можно вынести в отдельную функцию, что-то наподобии serialize_guy_position(). Под сериализацией в программировании понимается превращение чего-либо в строку.


function update()
    - зачем тебе else? Он же ничего не меняет. Или я не прав?
    - Можно код оставшийся вынести в check_coin().


function print(m)
    - зачем этой функции аргумент? Он же не используется внутри функции.
    - зачем внутри вызывать money(); и add_bomb(); Функция print просто же печатает матрицу слева в текстарие. А не монетки показывает.


function guy()
    - давай назовем ее show_guy
    - аналогично должны быть функции show_bomb b show_coin
    - когда ты используешь слово show, то чразу понятна, что данная функция что-то отображает, так как show - это показать, отобразить. А эта функция именно отображает человечка.

function del_guy()
    - hide_guy, альтернатива, но del_guy тоже ок

function chek_win()
    - 40 это константа :)

Общее:
    - все монетки и бомбы должны отображаться в самом начале один раз при инициализации. А потом исчезать, когда человек на них попадет.
    - читай про динамическую работу с документом :)
